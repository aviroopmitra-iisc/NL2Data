# =============================================================================
# NL2Data Configuration File
# =============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit the .env file to version control (it's in .gitignore)

# =============================================================================
# LLM Configuration
# =============================================================================
# Choose ONE of the following LLM provider options:

# Option 1: Google Gemini (Recommended)
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.0-flash-exp
# Available models: gemini-2.0-flash-exp, gemini-1.5-pro, gemini-1.5-flash

# Option 2: OpenAI (Legacy support)
# Get your API key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=your_openai_api_key_here
# MODEL_NAME=gpt-4
# Available models: gpt-4, gpt-4-turbo, gpt-3.5-turbo

# Option 3: Local LLM (OpenAI-compatible API)
# For local models like Ollama, LM Studio, etc.
# LLM_URL=http://127.0.0.1:1234
# MODEL=qwen3-vl-8b-instruct
# Example URLs:
#   - Ollama: http://localhost:11434/v1
#   - LM Studio: http://localhost:1234/v1
#   - vLLM: http://localhost:8000/v1

# =============================================================================
# LLM Behavior Settings
# =============================================================================
# Temperature controls randomness (0.0 = deterministic, 1.0 = very random)
TEMPERATURE=0.2

# LLM Timeout Configuration (in seconds)
# Increase for large/complex datasets that take longer to process
LLM_TIMEOUT=1800.0
LLM_MAX_RETRIES=3
LLM_RETRY_DELAY=5.0

# =============================================================================
# Data Generation Configuration
# =============================================================================
# Random seed for reproducibility (use same seed to get same results)
SEED=7

# Output directory for generated datasets
OUTPUT_DIR=output

# Number of rows to generate per chunk (for large datasets)
# Lower values use less memory but may be slower
CHUNK_ROWS=1000000

# =============================================================================
# Logging Configuration
# =============================================================================
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Optional: Specify a log file path (leave empty for console only)
# LOG_FILE=logs/nl2data.log

# =============================================================================
# Notes
# =============================================================================
# - All paths are relative to the project root unless absolute paths are used
# - Boolean values should be: true/false or 1/0
# - Numeric values can be integers or floats
# - String values don't need quotes unless they contain spaces
